<%= link_to item.program.name, :action => 'show', :controller => 'catalog', :id => item.program.id %>

<%= pluralize item.amount, "programa", "programas" %>, <%= sprintf "%0.2f €", item.price * item.amount %>

(<%= link_to '<b>-</b>'.html_safe, :controller => 'cart', :action => 'remove', :id => item.program %>)
